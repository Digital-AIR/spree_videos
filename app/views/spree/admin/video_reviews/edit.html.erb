<% content_for :page_title do %>
  <%= Spree.t(:editing_review_for_html, link: @video_review.product.name) %>
<% end %>
<%= form_for [:admin, @video_review] do |f| %>
  <div class="row">
    <div class="col-md-6">
      <%= render 'form', f: f %>
    </div>
    <div class="col-md-6">
      <% unless @video_review.title.blank? %>
        <h2><%= @video_review.title %></h2>
      <% end %>
      <blockquote>
        <%= simple_format(@video_review.review) %>
      </blockquote>
    </div>
  </div>
  <%= render 'spree/admin/shared/edit_resource_links' %>
<% end %>